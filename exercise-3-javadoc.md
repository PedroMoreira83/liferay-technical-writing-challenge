# Exercise 3: Javadoc for StringUtil.insert

Below is the Javadoc for the `insert` method, following standard Javadoc conventions.

```java
/**
 * Inserts a string into another string at a specified offset.
 * * If the original string {@code s} is {@code null}, the method 
 * returns the string literal "null".
 *
 * @param s the original string where the text will be inserted
 * @param insert the string to be inserted
 * @param offset the character index at which to insert the text
 * @return a new string with the inserted text, or "null" if the 
 * original string is null
 */
public static String insert(String s, String insert, int offset) {
    // Method implementation
}

